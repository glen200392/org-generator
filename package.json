{
  "name": "orgchart",
  "private": true,
  "version": "1.0.0",
  "description": "OrgChart monorepo: SAP Edition + Studio + shared core",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "legacy:serve": "pnpm --filter @orgchart/legacy serve"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
